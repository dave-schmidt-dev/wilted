# Transcript interface

`interface-v1.json` is the immutable Lane 1 handoff for platform consumers. It
documents the read-only domain interface, local migration, CloudKit mapping,
limits, and fixture states. Production schema promotion is deliberately outside
this contract artifact.
